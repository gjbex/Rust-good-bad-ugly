[package]
name = "polars-data-analysis"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.4", features = ["derive"] }
polars = { version = "0.54.4", default-features = false, features = [
    "csv",
    "fmt",
    "lazy",
    "parquet",
] }

