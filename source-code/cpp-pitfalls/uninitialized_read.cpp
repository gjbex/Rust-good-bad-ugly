#include <iostream>

int main() {
    int value;

    if (value > 0) {
        std::cout << "positive value: " << value << '\n';
    } else {
        std::cout << "non-positive value: " << value << '\n';
    }

    return 0;
}
