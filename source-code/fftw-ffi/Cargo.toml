[package]
name = "fftw-ffi"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.4", features = ["derive"] }
fftw-sys = { version = "0.8.0", default-features = false, features = ["system"] }
num-complex = "0.4.6"
